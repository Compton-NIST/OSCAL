name: 'Workflow Monitor'
description: 'This action captures information about the current state of workflows for troubleshooting.'
runs:
  using: "composite"
  steps:
  - name: "***** Check current rate limits. *****"
    run: |
      echo "\n\n###########################################################################\n"
      curl -I https://api.github.com/users/oscalbuilder | grep -E "x-ratelimit-remaining|x-ratelimit-limit|x-ratelimit-used]"
      echo "\n###########################################################################\n\n\n"
    shell: bash